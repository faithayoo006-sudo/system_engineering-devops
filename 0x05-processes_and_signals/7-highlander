#!/usr/bin/env bash
# A Bash script that continuously displays "To infinity and beyond" and prints "OK" when receiving SIGTERM
# Trap SIGTERM and print OK, then exit
trap 'echo "OK"; exit 0' SIGTERM
while true; do
    echo "To infinity and beyond"
    sleep 2
done
