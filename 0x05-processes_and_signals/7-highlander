#!/usr/bin/env bash
# Continuous loop that prints "OK" when receiving SIGTERM

# Trap SIGTERM, print exactly "OK" and exit
trap 'printf "OK\n"; exit 0' SIGTERM

# Infinite loop to keep the process alive
while true; do
    sleep 1
done
