#!/usr/bin/env bash
# Continuously displays "To infinity and beyond" and prints "OK" on SIGTERM
# Trap SIGTERM and print exactly OK, then exit
trap 'printf "OK"; exit 0' SIGTERM
while true; do
    printf "To infinity and beyond\n"
    sleep 2
done
