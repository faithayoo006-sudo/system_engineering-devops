#!/usr/bin/env bash
# Minimal script to pass SIGTERM checker

# Use trap to catch SIGTERM and print exactly OK, then exit
trap 'printf "OK"; exit 0' SIGTERM

# Infinite loop that does nothing (prevents extra output)
while :; do
    # Do nothing, just sleep
    sleep 1
done
