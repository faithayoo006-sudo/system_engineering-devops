#!/bin/bash
#!/usr/bin/env bash
# This script lists all non-hidden files showing only the part after the first dash (-)
for file in *; do
    if [[ -f "$file" ]]; then
        echo "${file#*-}"
    fi
done
